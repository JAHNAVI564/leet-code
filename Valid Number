#include <regex>

class Solution {
public:
    bool isNumber(string s) {
        // Updated regex pattern to match both 'e' and 'E'
        regex pattern("^[+-]?((\\d+\\.\\d*)|(\\.\\d+)|(\\d+))([eE][+-]?\\d+)?$");

        return regex_match(s, pattern);
    }
};
